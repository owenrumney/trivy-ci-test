# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Linux

parameters:
  - name: platform
    type: string
    default: linux

jobs:
- job: scan_image
  steps:
  - task: trivy-dev@2
    name: 'trivy_${{ parameters.platform }}'
    inputs:
      version: 'latest'
      type: 'filesystem'
      target: '.'
      scanners: 'vuln,misconfig'
      showSuppressed: true
      trivyUrl: https://get.trivy.dev/trivy?os=linux&arch=amd64
      reports: cyclonedx
      publish: true

  - task: Bash@3
    displayName: "Print Trivy CycloneDX report"
    condition: always()    # ensures it runs even if Trivy failed
    inputs:
      targetType: 'inline'
      script: |
        echo "🔍 Checking Trivy CycloneDX report variable..."
        echo "Report path: $(trivy_${{ parameters.platform }}.cyclonedxReport)"
        echo ""

        if [ -f "$(trivy_${{ parameters.platform }}.cyclonedxReport)" ]; then
          echo "------ 📄 CycloneDX Report Contents ------"
          cat "$(trivy_${{ parameters.platform }}.cyclonedxReport)"
          echo "-----------------------------------------"
        else
          echo "⚠️ File not found at path: $(trivy_${{ parameters.platform }}.cyclonedxReport)"
        fi